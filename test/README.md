Tests to check if the Caddyfile still does, what was intended. The docker setup should run on the same machine.
Adapt `BASE_DOMAIN` as needed.

To execute the tests, run

```
python3 -m unittest
```

Add `--help` to see more options.
